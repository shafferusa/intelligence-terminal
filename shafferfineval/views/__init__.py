"""
ShafferFinEval views.

Named `views/` rather than `pages/`: Streamlit treats a top-level `pages/`
directory as an auto-multipage app and would execute every module in it as its
own script. These are render functions called by app.py, not scripts.
"""
