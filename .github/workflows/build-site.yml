name: Build & deploy site

on:
  push:
    branches: [main]
    paths:
      - "site/**"
  workflow_dispatch:
  # PR-path publishes: after publish-report merges a claude/* report PR, this
  # workflow_run trigger rebuilds and deploys from main (merges made with
  # GITHUB_TOKEN never fire push events, and the github-pages environment only
  # allows deploys from main — which workflow_run satisfies).
  workflow_run:
    workflows: ["Publish report (auto-merge claude/ PRs)"]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write
  pull-requests: read   # notify gate: look up the merged PR's file list

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # state/ lives outside the served site root, so the status page cannot
      # fetch the run log directly. Copy it in at build time.
      - name: Publish run log for the status page
        run: cp state/run-log.jsonl site/status.jsonl || echo "no run log yet"

      # site/assets/* is served cache-first by the service worker, so an
      # installed PWA keeps the old file until the cache KEY changes. Bumping
      # that key by hand was the documented rule and it was missed the very
      # next time assets changed: a report.js update shipped while sw.js still
      # said ldn-cache-v2, so every installed phone kept running the old file.
      #
      # So derive it instead. The key is now a hash of the asset bytes: change
      # any asset and the cache key changes with it, forget to do anything and
      # it still works. Only the deployed copy is rewritten; the value in the
      # repo stays a readable fallback.
      - name: Stamp the service-worker cache key with an asset hash
        run: |
          HASH=$(find site/assets -type f -exec sha256sum {} \; \
                 | sort | sha256sum | cut -c1-12)
          sed -i "s/^var VERSION = \".*\";/var VERSION = \"ldn-cache-$HASH\";/" site/sw.js
          echo "cache key: ldn-cache-$HASH"
          grep -n '^var VERSION' site/sw.js

      - name: Build Pagefind search index
        run: npx -y pagefind --site site

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Telegram notification for PR-path publishes only — sent AFTER the deploy so the
  # link is live. Direct-push publishes are notified by notify-telegram.yml instead.
  #
  # GUARD (2026-09-25): only notify when the merged PR actually published a new
  # edition, i.e. its own file list includes site/reports/index.json. Without this,
  # any workflow_run re-announced the latest edition and sent a duplicate brief.
  # publish-report.yml now only merges routine PRs, so this is the second line of
  # defence. It reads the PR's files, NOT main's tip: other commits (the sie-inbox
  # poller, a direct-push edition) can land on main before this job starts, and a
  # tip diff would then skip a real edition.
  notify:
    if: github.event_name == 'workflow_run'
    needs: deploy
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Did the merged PR publish a new edition?
        id: gate
        env:
          GH_TOKEN: ${{ github.token }}
          # publish-report ran on the pull_request event, so this is the PR's head commit.
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          pr=$(gh api "repos/${{ github.repository }}/commits/$HEAD_SHA/pulls" \
                 --jq '[.[] | select(.merged_at != null)][0].number // empty' || true)
          if [ -z "$pr" ]; then
            # Fail open: a missed edition is worse than a rare duplicate.
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "could not resolve the merged PR for $HEAD_SHA -- notifying anyway."
            exit 0
          fi
          if ! files=$(gh api --paginate "repos/${{ github.repository }}/pulls/$pr/files" --jq '.[].filename'); then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "could not list PR #$pr files -- notifying anyway."
            exit 0
          fi
          if printf '%s\n' "$files" | grep -qx 'site/reports/index.json'; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "PR #$pr changed index.json -- a new edition, will notify."
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "PR #$pr did not change index.json -- no new edition, skipping Telegram."
          fi

      - name: Send Telegram notification
        if: steps.gate.outputs.changed == 'true'
        env:
          TG_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_CHAT: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python3 .github/scripts/notify.py
